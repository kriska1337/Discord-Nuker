# 👉 Discord Token Nuker (BETA VERSION)

☠️ **Made by MoonRise Network**  
💫 **Important:** This is a beta version, so no claims can be made against me!

---

## ⚡️ Installation

1. Install the required library:
   ```bash
   pip install discord.py-self
   ```

2. **Warning:** If you have `discord` or `nextcord` libraries installed, the nuker will not work.  
   To fix this, uninstall them:
   ```bash
   pip uninstall discord
   pip uninstall nextcord
   ```

3. In the code, replace `YOUR_TOKEN` with the victim's token.
